# Shared Shell Notes

Keep reusable shell notes here only when they are independent of both active
VibeShell and the read-only Nandriod reference. Do not move active VibeShell
modules into this folder without a dedicated migration.
