# VibeShell Profile

The active VibeShell implementation remains under:

```text
/etc/nixos/asuraPc/vibeshell
```

Use `switch-shell vibeshell` to stop managed Nandriod instances and start the
current VibeShell launcher.
